# Wireless_ADB_Automated
A simple yet powerfull linux shell script to automatically find and connect to an android device connected on same network
# Usage
1. Install dependencies first --> $ sudo apt update && sudo apt install adb fastboot nmap

2. change your directory to where your downloaded script is

3. run as root
